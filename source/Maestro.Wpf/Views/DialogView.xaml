<UserControl x:Class="Maestro.Wpf.Views.DialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Maestro.Wpf"
             xmlns:viewModels="clr-namespace:Maestro.Wpf.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:DialogViewModel}"
             Background="{x:Static local:Theme.BackgroundColor}"
             Width="Auto"
             Height="Auto"
             Padding="4">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding Message}" />
        <Button Grid.Row="1" Grid.Column="0" Content="Confirm" Command="{Binding ConfirmCommand}" />
        <Button Grid.Row="1" Grid.Column="2" Content="Cancel" Command="{Binding CloseCommand}"/>
    </Grid>
</UserControl>
