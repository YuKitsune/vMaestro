<UserControl x:Class="TFMS.Wpf.TFMSView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TFMS.Wpf"
             xmlns:controls="clr-namespace:TFMS.Wpf.Controls"
             xmlns:converters="clr-namespace:TFMS.Wpf.Converters"
             mc:Ignorable="d"
             Background="{x:Static local:Theme.BackgroundColor}"
             d:DesignHeight="800"
             d:DesignWidth="800">
    <UserControl.DataContext>
        <local:TFMSViewModel />
    </UserControl.DataContext>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TFMS.Wpf;component/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <converters:TotalSecondsConverter x:Key="TotalSecondsConverter"/>

            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource ButtonBaseStyle}" />

        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="16" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="42.267" />
            <RowDefinition Height="5.733"/>
            <RowDefinition Height="24" />
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>

        <!-- Unknown Buttons -->
        <Grid Grid.Column="0" Grid.Row="0" Grid.RowSpan="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Button Grid.Row="0"/>
            <Button Grid.Row="1"/>
        </Grid>

        <!-- Header -->
        <controls:BeveledBorder 
            Grid.Column="1"
            Grid.Row="0"
            BevelType="Outline"
            BorderThickness="{x:Static local:Theme.BeveledBorderThickness}"
            Margin="1,1,1,1" Grid.RowSpan="2">

            <Grid Margin="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="72" />
                    <ColumnDefinition Width="6" />
                    <ColumnDefinition Width="56" />
                    <ColumnDefinition Width="6" />
                    <ColumnDefinition Width="72" />
                    <ColumnDefinition Width="6" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="6" />
                    <ColumnDefinition Width="72" />
                </Grid.ColumnDefinitions>

                <!-- Sector ID -->
                <TextBlock
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    TextWrapping="Wrap"
                    HorizontalAlignment="Center">
                        FLOW
                </TextBlock>

                <controls:BeveledLine Grid.Column="1" Width="{x:Static local:Theme.BeveledLineWidth}" Orientation="Vertical"/>

                <!-- Aerodrome Selector -->
                <ComboBox
                    Grid.Column="2"
                    Padding="5"
                    ItemsSource="{Binding Path=AvailableAirports}"
                    SelectedItem="{Binding Path=SelectedAirport}"
                    DisplayMemberPath="Identifier" />

                <controls:BeveledLine Grid.Column="3" Width="{x:Static local:Theme.BeveledLineWidth}" Orientation="Vertical"/>

                <!-- Runway Selector -->
                <ComboBox
                    Grid.Column="4"
                    Padding="5"
                    ItemsSource="{Binding Path=AvailableRunwayModes}"
                    SelectedItem="{Binding Path=SelectedRunwayMode}"
                    DisplayMemberPath="Identifier" />

                <controls:BeveledLine Grid.Column="5" Width="{x:Static local:Theme.BeveledLineWidth}" Orientation="Vertical"/>

                <!-- Runway Intervals -->
                <controls:BeveledBorder Grid.Column="6" BorderThickness="{x:Static local:Theme.BeveledBorderThickness}" BevelType="Raised">
                    <ItemsControl ItemsSource="{Binding Path=RunwayRates}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text=" - "/>
                                    <TextBlock Text="{Binding Identifier}"/>
                                    <TextBlock>:</TextBlock>
                                    <TextBlock Text="{Binding LandingRate, Converter={StaticResource TotalSecondsConverter}}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </controls:BeveledBorder>

                <controls:BeveledLine
                    Grid.Column="7"
                    Width="{x:Static local:Theme.BeveledLineWidth}"
                    Orientation="Vertical" />

                <!-- Setup Button -->
                <Button Grid.Column="8" Content="SETUP" />
            </Grid>

        </controls:BeveledBorder>

        <!-- Tab Bar -->
        <StackPanel 
            Grid.Column="1"
            Grid.Row="2"
            Orientation="Horizontal"
            Margin="1,1,1,1">
            <StackPanel.Resources>
                <Style TargetType="{x:Type Button}" BasedOn="{StaticResource ButtonBaseStyle}">
                    <Setter Property="MinWidth" Value="56" />
                    <Setter Property="Margin" Value="0,0,2,0"/>
                </Style>
            </StackPanel.Resources>
            <Button>
                <TextBlock>DEPS</TextBlock>
            </Button>
            <Button>
                <TextBlock>DESQ</TextBlock>
            </Button>
            <ItemsControl ItemsSource="{Binding Path=AvailableSectors}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding SelectSectorCommand}" CommandParameter="{Binding .}">
                            <TextBlock Text="{Binding Identifier}" />
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Button>
                <TextBlock>RWY</TextBlock>
            </Button>
        </StackPanel>

        <!-- Scroll Bar -->
        <Grid Grid.Column="0" Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Margin="0,0,0,1">
                <Path 
                    Fill="{x:Static local:Theme.InteractiveTextColor}"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Data="M3,0 L6,6 L0,6 z"/>
            </Button>
            <Button Grid.Row="1" Margin="0,1,0,1"></Button>
            <Button Grid.Row="2" Margin="0,1,0,0">
                <Path 
                Fill="{x:Static local:Theme.InteractiveTextColor}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Data="M3,6 L0,0 L6,0 z"/>
            </Button>
        </Grid>

        <!-- Ladder -->
        <Canvas
            Grid.Column="1"
            Grid.Row="3"
            Name="LadderCanvas" 
            Margin="1,1,1,1"
            ClipToBounds="True" />

        <!-- Footer -->
        <Grid Grid.Column="1"  Grid.Row="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="104"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
            </Grid>

            <ItemsControl Grid.Column="0" ItemsSource="{Binding Path=LeftFeederFixes}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding .}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <controls:BeveledBorder Grid.Column="1" BevelType="Outline" BorderThickness="{x:Static local:Theme.BeveledBorderThickness}">
                <TextBlock x:Name="ClockText"/>
            </controls:BeveledBorder>

            <ItemsControl Grid.Column="2" ItemsSource="{Binding Path=RightFeederFixes}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding .}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</UserControl>
